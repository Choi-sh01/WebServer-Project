spring:
  datasource:
    url: jdbc:mysql://localhost:3306/localletter?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root  # ? ? MySQL ????
    password: y3274012  # ? ? MySQL ????
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # ??? update, ??? validate? none?? ??? ?
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    cache: false  # JSP ?? ?? ?? ??? ?? ?? ?? ??

server:
  port: 8080
